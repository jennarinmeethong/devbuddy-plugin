# Verification Matrix

This file is the **single source of truth for whether a security control actually works**. A row
moves out of `NOT IMPLEMENTED` only when the named test exists, runs in CI, and passes. Nothing
else — not a design document, not a code review, not an intention — changes a row.

**Current state: 33 controls. 33 `TESTED`, 0 `IMPLEMENTED`, 0 `NOT IMPLEMENTED`.**

Controls are defined in [security-baseline.md](security-baseline.md); threats in
[threat-model.md](threat-model.md).

## Status values

| Value | Meaning |
|---|---|
| `NOT IMPLEMENTED` | No code exists for this control. |
| `IMPLEMENTED` | Code exists, or part of the verification passes. **Not proven.** Never report a control as done at this status. The Evidence column says exactly which part passes and which is outstanding. |
| `TESTED` | An automated test or a recorded drill exercises the control and passes in CI. |
| `GAP` | Implemented but the verification failed or was skipped. Must be stated in release notes. |

## Rules

1. Only a passing test or a recorded drill moves a row to `TESTED`.
2. If a test is deleted, disabled, or quarantined, the row returns to `GAP` in the same commit.
3. A release note lists every row that is not `TESTED`, verbatim. Silence is not permitted.
4. `docs/plan.md` phase exit criteria reference this file; a phase does not complete while a
   control it claimed is below `TESTED`.

---

## Matrix

| ID | Control (short) | Phase | Verification | Status | Evidence |
|---|---|---|---|---|---|
| SB-01 | Untrusted content handled as data | 6 | Injection corpus test | **TESTED** | `PromptInjectionCorpusTests` analyses a repository containing exfiltration instructions, hidden HTML comments addressed to an assistant, and a metadata-service URL. All of it comes back as text, and the AI tool surface is byte-identical afterwards. |
| SB-02 | Permissions enforced in code, not prompts | 6, 7, 9 | Tool-surface test without instruction files | **TESTED** | The injection corpus compares the AI-exposed operation list before and after analysing hostile content and finds it unchanged. Instruction text is not an input to any decision the system makes. Phase 9 adds the other direction: `PluginPackageTests` fails if either shipped instruction file so much as names an operation people alone may perform, so a package cannot teach an assistant that a name exists, and both files are checked to still say that prompt text is not a security boundary. |
| SB-03 | Network destination allow-list | 6 | Egress test | **TESTED** | `UrlGuardTests`: nothing is reachable with an empty allow-list, which is the default. The analyser has no HTTP client at all, and the corpus proves a URL found in a document is reported rather than fetched. |
| SB-04 | No repository execution | 6 | Hostile-hooks repository, process-spawn assertion | **TESTED** | Two ways. Behaviourally: a fixture with a build script, a Makefile, an npm preinstall hook, and an MSBuild pre-build target, each of which would leave a marker file; every analysis kind runs and the marker never appears. Structurally: `NoExecutionTests` scans the product source and fails the build if any process or dynamic-loading API appears. |
| SB-05 | Path traversal and symlink escape rejected | 3, 6 | Path-traversal corpus | **TESTED** | `PathGuardTests` covers relative escapes, absolute paths outside the root, a null byte, a sibling directory sharing the root prefix, a symlink pointing out, and a file reached *through* a symlinked parent directory. Resolution happens before comparison, and it resolves **every component** rather than the last one. That last part was a real escape, found the first time the suite ran on Linux in CI: `File.ResolveLinkTarget` returns null for a path whose leaf is an ordinary file, so `escape/secret.txt` behind a linked `escape` directory still began with the root and passed. Windows needs developer mode to create a link at all, so the covering test had been taking its fallback branch on the development machine and the suite was green. Mutation-checked: restoring the leaf-only version fails both the new test and the one that existed. |
| SB-06 | SSRF prevention incl. redirects and DNS | 6 | SSRF corpus | **TESTED** | `UrlGuardTests`: loopback, link-local, the cloud metadata address, private and carrier-grade ranges, IPv6 forms, an IPv4-mapped loopback, non-HTTP schemes, and a host resolving to both a public and a private address. Every resolved address is checked, not only the first. |
| SB-07 | MCP tool allow-list, both transports | 2, 7, 9 | Tool-surface equality test per transport | **TESTED** | `ToolSurfaceTests` asserts the exported tool list *equals* the eighteen-name allow-list — not contains, not excludes — against a list written out independently of the catalogue, so a change has to be made twice, and a source scan fails the build if anything outside `ToolSurface.cs` constructs a `Tool`. Phase 9 adds the transport itself: `StdioTransportTests` launches the real host process, speaks the MCP handshake over the pipes, and gets the same eighteen back — which is also what catches a logger writing to stdout and corrupting the protocol. |
| SB-08 | AI access denied by default per project | 4, 7 | AI-disabled project returns nothing via MCP | **TESTED** | The named verification, exactly: `CrossSurfaceTests.ai_access_off_returns_nothing_to_mcp_while_a_human_reads_the_same_record` reads one record over real HTTP as an authorised person and gets it, calls the same tool on the AI channel against the same container and is refused, then enables the policy and gets it. The third step is what makes the second evidence rather than a broken path. |
| SB-09 | Results narrowed to requesting user | 4, 7, 9 | Two-user differential MCP query | **TESTED** | `with_ai_access_on_two_callers_get_different_answers_through_mcp`: with the project opted in, a member reads the record through the MCP handlers, and an account with no membership calls the same tool with the same arguments and is refused. `StdioPluginTests` carries it into the shape a plugin actually runs in: a child process presenting a machine token acts as that token's owner and nobody else, and an administrator's token still cannot reach a human-gated operation because a token carries permissions, not a channel. |
| SB-10 | Human-gated operations absent from AI surface | 2, 7 | Absence test | **TESTED** | Absent, and refused if named anyway. `no_human_gated_operation_appears_on_the_surface` checks every Denied operation against the exported list; `calling_a_human_gated_operation_by_name_is_answered_as_unknown` shows `publish_record` gets the same answer as an operation nobody defined, with no mention of permission; and `a_human_only_operation_is_refused_on_the_ai_channel_even_with_ai_access_on` shows that a workspace administrator on the AI channel is still on the AI channel. |
| SB-11 | Server-side authorization every request | 2, 4 | Cross-user/team/project access tests | **TESTED** | `TenantIsolationTests` and `RevokedAccessAndRoleTests` run the real pipeline over the real authorization service against PostgreSQL: cross-user, cross-team, cross-project, and disabled-account cases all denied. The authorization service now also checks the ceiling the presented credential carries, before it looks the caller up at all: a machine token names one workspace, and a request naming another is refused without reaching the membership check it would have passed. A signed-in person's session carries no such ceiling and is unaffected, which `a_signed_in_person_over_http_is_not_narrowed_by_any_machine_token_scope` holds in place. Mutation-checked: removing the credential check leaves three cross-workspace tests failing. |
| SB-12 | Isolation covers search, exports, attachments, caches | 3, 4 | Isolation suite over each path | **TESTED** | All four paths covered end to end in `TenantIsolationTests`: record read, search, export, and attachment download. Attachments are now covered for evidence a person actually attached rather than only for evidence a test seeded — `EvidenceCaptureTests.evidence_captured_in_one_project_is_not_reachable_from_another` captures through the pipeline and then asks for the same identifier through the other project scope. That distinction mattered: until `capture_evidence` existed, nothing outside a test could put an artefact in the store at all. There is no cache layer yet; if one is added this row returns to `IMPLEMENTED` until it is covered too. |
| SB-13 | Password storage, lockout, rate limiting | 4, 7 | Lockout and rate-limit tests | **TESTED** | All three. Hashing and lockout in `AccountSecurityTests`, and over HTTP in `repeated_wrong_passwords_lock_the_account_out`, including that the right password does not lift a lockout. Rate limiting in `the_authentication_endpoints_are_rate_limited`, which runs a second instance of the real application with a small budget and sees the excess refused with 429. The limiter is partitioned by remote address, so exhausting it does not lock everybody else out. |
| SB-14 | Token lifetime, rotation, revocation | 4, 9 | Revoked-token and rotation-replay tests | **TESTED** | `AccountSecurityTests`: rotation, reuse detection revoking the whole family, single revoke, revoke-all, and expiry, all stored only as a hash; the same over HTTP in `AuthenticationEndpointTests`. Phase 9 adds machine tokens, which are a separate table precisely because they are meant to be presented repeatedly and a non-rotating row in the refresh table would have retired reuse detection: `revoking_a_token_stops_it_on_the_next_call` proves revocation is immediate rather than effective at the next process restart. A machine token is also bound to one workspace, so its reach is bounded as well as its lifetime: `MachineTokenScopeTests` runs fourteen cases through the real pipeline against real PostgreSQL — a token works where it was minted and is refused in a second workspace its owner is equally a member of, in both directions; one person holds a working token per workspace and several within one; a listing never shows another workspace's token and revoking one answers not found; revoking one token leaves the others working; and unknown, expired, revoked and unscoped tokens all resolve to nobody. `StdioPluginTests` carries the same boundary into the process shape a plugin runs in. **Residual risk:** the server issues bearer tokens rather than setting an HttpOnly cookie, so the web client holds a refresh token in `sessionStorage`, where any script reaching the page can read it. Bounded to one tab and cleared when it closes; the fix is a cookie-based refresh, and that is a server change. |
| SB-15 | Safe account recovery | 4 | Reuse, expiry, and enumeration tests | **TESTED** | `AccountSecurityTests`: single-use, expiry, a new request retiring the previous token, no account enumeration, and recovery signing every existing session out. **Phase 12B closed the delivery hole underneath it:** with no SMTP configured, v1 wrote the recovery token itself into the application log unconditionally, so a single-use credential sat in a file for as long as the log did, and `release-readiness.md` carried that as an accepted risk rather than as a control. `Email:AllowTokensInLog` is false now, and `EmailSenderTests` asserts all four halves of that — the body and the token are absent by default, present only with the opt-in set, the withheld line names both ways to make the token reachable, and the type's default is off rather than a comment claiming so. The intended consequence, stated because a closed risk that quietly breaks a workflow is not closed: with neither SMTP nor the opt-in, a recovery token is generated, is unreachable, and expires. |
| SB-16 | Role enforcement server-side | 4, 7, 8 | Role matrix test per endpoint | **TESTED** | The exhaustive role-by-permission matrix runs against the real pipeline (`RolePermissionsTests`, `RevokedAccessAndRoleTests`), including that a project grant does not reach a workspace-level operation. Per endpoint collapses to per operation here on purpose: the API has one dispatch route rather than forty hand-written ones, so there is no endpoint that could hold a different opinion. `a_viewer_cannot_reach_the_approval_or_audit_surfaces` drives a real viewer session over HTTP and is refused approval, correction, publication, archiving, audit reading, membership listing, project creation, and account creation. The UI hides those, and the smoke test checks that it does; the refusal is the server's either way. |
| SB-17 | Secret detection and redaction, retention and egress | 2, 6 | Secret corpus at both points | **TESTED** | A real scanner and redactor sharing one rule set. `SecretCorpusTests` covers eight secret shapes plus negatives that must survive untouched; `RetentionAndEgressTests` proves a draft carrying a credential is refused with nothing stored, that every retained field is scanned, and that a secret already in the database is redacted on the way out. `EvidenceCaptureTests` extends the retention half to attachments, which are the material most likely to carry one by accident: a log holding an access key is Blocked and the store is left empty, because capture scans before it writes rather than writing and then cleaning up. `EvidenceEndpointTests` shows the same refusal over real HTTP as a 422 naming the rule that matched. Accepted limitation AL-2 still applies: this catches known shapes and high entropy, not everything. |
| SB-18 | Customer/production/personal data denied by default | 6, 7, 11 | Policy test incl. approved bounded scope | **TESTED** | `PersonalDataPolicyTests` proves the pipeline gating against fakes: the AI channel with no bounded scope blocks personal data; an approved bounded scope lets it through; the human channel is never subject to the control; and a secret is still refused inside an approved scope. `PersonalDataChannelTests` proves the same against real PostgreSQL: a human can draft a customer SSN on their own project; the AI channel is refused it until the owner approves a bounded scope; a read over the AI channel redacts personal data (label kept, value removed) unless a scope is approved; and the human channel is never redacted for it. `PersonalDataCorpusTests` covers detection: a US SSN, Luhn-checked payment card numbers, and labelled fields (date of birth, passport number), held to the same corpus discipline as `SecretCorpusTests`. |
| SB-19 | Audit stores no sensitive payload | 2, 5 | Audit content test | **TESTED** | `LifecycleAndAuditTests.the_audit_trail_never_contains_the_content_it_describes` scans every audit row and detail column in real PostgreSQL for a marker that is present in the record body. Mutation-checked: adding the marker to an audit detail makes it fail. Detail values are capped at 200 characters in the domain and refused rather than truncated. |
| SB-20 | Provenance and history support correction | 1, 5 | Provenance invariant and correction flow | **TESTED** | Provenance is mandatory (`RecordRevisionTests`) and the correction flow runs end to end against real PostgreSQL (`a_record_travels_from_draft_through_correction_to_published`), with the reason retained on the stored record. |
| SB-21 | Size, rate, and concurrency limits | 3, 6, 10 | Oversized upload, flood, concurrency tests | **TESTED** | All four now exist and three are exercised directly: evidence size (`an_oversized_object_is_refused_and_nothing_is_written`), the analysis file, size and time ceilings (`AnalysisLimitsTests`), and the credential rate limit over HTTP (`the_authentication_endpoints_are_rate_limited`). Phase 10 added the two that were missing: a global per-caller request limit — per account when signed in, per address when not, so one runaway script cannot slow everybody down — and a process-wide cap on concurrent analyses, enforced by a decorator around the port so a second implementation cannot forget it. A request body ceiling refuses an oversized upload at the edge rather than after buffering it. |
| SB-22 | Analysis execution time limit | 6 | Pathological input test | **TESTED** | `AnalysisLimitsTests`: a spent budget returns a report saying the answer is incomplete rather than throwing, a caller cancellation still propagates, the file ceiling bounds the walk, an oversized file is listed but not read, and a 60-deep tree finishes. A non-positive timeout means no time, not unlimited. |
| SB-23 | Approval bound to exact revision | 1, 5 | Domain invariant + stale-approval e2e | **TESTED** | Both halves pass. End to end against real PostgreSQL: publishing after the text changed is rejected and audited, and approving a hash that is no longer current is rejected and audited. |
| SB-24 | Revisions immutable | 1, 3 | Immutability tests, domain and persistence | **TESTED** | Both halves pass against real PostgreSQL: `RecordRevisionTests` and `PersistenceTests.rewriting_a_stored_revision_is_refused`. |
| SB-25 | Provenance mandatory, snapshots retained | 1, 3 | Invariant + snapshot round-trip | **TESTED** | Invariant and jsonb round-trip both pass: `RecordRevisionTests`, `PersistenceTests.a_published_record_round_trips_with_its_whole_history`. |
| SB-26 | Drafts separated from published everywhere | 1, 5, 7, 8 | Draft-visibility test across API, MCP, UI | **TESTED** | Three places. At the pipeline: `a_reader_of_published_knowledge_never_sees_the_draft_that_follows_it`. Over HTTP: `a_draft_written_after_publication_is_not_what_a_reader_gets` writes an unapproved revision on top of a published record and shows the reader still gets the approved text, with the listing naming which revision is live. On the AI channel: `an_unapproved_revision_is_not_served_to_the_ai_channel_either`, which is the surface that matters most for it. The UI is covered by construction rather than by a fourth test — `get_record` is its only source for a record body, so it cannot show what that operation will not serve. |
| SB-27 | Retention applies to all data copies | 11 | Purge tests per copy + deleted-project sweep | **TESTED** | `RetentionEnforcementTests` proves four copies against real PostgreSQL and the filesystem: an audit event past its 24-month window is deleted while a recent one survives; evidence no revision references is deleted from both the database row and the object store once its 90-day grace period passes, while referenced-but-old and recent-but-unreferenced evidence both survive; a backup directory past its configured window is deleted from disk while a recent one survives — the gap `docs/plan.md` once named as recorded but not enforced; and an export directory past its window is deleted the same way, now that `export_project` writes an actual copy (`ExportProjectTests`) instead of only a manifest. An archived record past its window is reported eligible, not deleted, matching the schedule's "on owner request." Applied by `dotnet run -- retention`, an operator-scheduled console command outside the pipeline, the same shape as `restore`. The deleted-project sweep named in the original verification is unneeded rather than missing: `delete_project` (`ProjectDeletionTests`) purges a project's rows and evidence bytes immediately, so no lagging state exists for a sweep to find. Application log retention is enforced here too, since the log sink landed: a rolled log file past `Logging:File:RetentionDays` is deleted by the same sweep, while a recent one and the live file that has not rolled yet both survive (`a_rolled_log_file_past_its_window_is_deleted_and_a_recent_one_survives`). Serilog also drops files as it rolls, which covers a running service; the sweep covers one that has been stopped, and is the half a test can drive. That was the last row of the ten-row schedule sitting outside this codebase. It is still switchable — clearing `Logging:File:Path` reverts to the container log driver's size bound — and that is now the only part left to the operator. **Whether anything runs the sweep on a schedule is no longer one of them (Phase 12B):** `docker/compose.yaml` ships a `retention` service running `retention --every 24h` in the console image, and `DeploymentTests` fails if that service or its interval goes. `RetentionScheduleTests` is twenty-seven cases over the loop and asserts the thing worth asserting — that the scheduled pass and the single pass call the identical delegate and produce the identical report, so this is one sweep with two ways to start it rather than two paths that agree today. It also covers the parts that were decided rather than defaulted: the first pass is immediate, a failed pass is reported and the loop continues, a cancelled schedule takes no further pass, and `--every 24` is refused rather than read as twenty-four days. Verified running as well as configured: `--every 1m` in a container against real PostgreSQL produced two passes a minute apart, and `docker stop` returned in under a second with exit code 0 and the schedule's closing line in the log, so SIGTERM is handled rather than a ten-second kill. The scheduler holds no actor, no membership and no tenant context, for the same reason `restore_system` does not exist. `docs/security/release-readiness.md` records what is left. Draft staleness is a report rather than a deletion and is already `DetectStaleness` from Phase 6. |
| SB-28 | Dependencies pinned and scanned | 0, 10 | CI vulnerability gate | **TESTED** | Pinned since Phase 0 by central package management, with transitive pinning on. Scanned by the `supply-chain` workflow: `dotnet list package --vulnerable --include-transitive` on every push and weekly, and `bun audit` for the web client. The gate is the text rather than the exit code, because `dotnet list` exits zero whatever it finds — without that the job would be a report nobody reads. The weekly run is the half that matters: a vulnerability is published against a dependency that has not changed. |
| SB-29 | SBOM and verifiable artifact origin | 10 | SBOM attached per release | **TESTED** | `v1.0.0` is published: run 34045844221, built from `9a8ebf0`. Three images in GHCR, each carrying its own CycloneDX SBOM attested under predicate `https://cyclonedx.org/bom` and distinct per host — 36, 38 and 56 components for the API, the MCP server and the console — and every archive signed with GitHub's keyless OIDC identity. Verified **from outside the workflow that made them**, after publication: provenance names this repository, `.github/workflows/release.yml`, `refs/tags/v1.0.0` and commit `9a8ebf0`; the `linux-x64` archive's attested digest matches the downloaded file byte for byte and matches `SHA256SUMS`; a deliberately wrong `--owner` is refused, so a pass means something. The release notes state verbatim which rows were not run — four platforms built but never run, and no `linux/arm64` image. **The second of those is closed (Phase 12B):** all three images now build for `linux/amd64` and `linux/arm64` under one tag, cross-compiled rather than emulated, and all three were started on arm64 — six migrations applied and eighteen AI-exposed operations listed by the console, `/health` 200 and `/operations` 401 from the API, `POST /` 401 from the MCP server — under emulation, recorded as emulated rather than as hardware. The workflow's non-root check runs per architecture, because a manifest list can hold one image that drops root and one that does not, and `DeploymentTests` fails if either the cross-compilation flags or the second platform goes. The four unrun RIDs stay, now as a confirmed decision (`info.md`, 2026-09-10) rather than as a gap. `docs/operations/release-matrix.md` holds the per-release checklist, and this row returns to `IMPLEMENTED` if a later release ships without one. **The workflow that does all of this is now itself verified without cutting a release (2026-09-10):** every action in it was a major version behind and on Node 20, and bumping them moved three actions on this row's own path — `attest-build-provenance`, `attest-sbom` and `download-artifact`. Two throwaway prerelease tags proved the result rather than assumed it. `v1.1.1-rc.1` found that `attest-sbom` is deprecated and that a prerelease draft was not marked as one, so publishing a release candidate would have made it this repository's Latest release; `v1.1.1-rc.2` proved both fixes, with `actions/attest@v4` producing the same CycloneDX attestation the deprecated action did — 36, 38 and 56 components, verified from outside against a wrong-owner control — and no annotations left in the run at all. The moving `{{major}}.{{minor}}` container tag was confirmed not to follow a prerelease, so nobody pinned to `1.1` is served a candidate. Verifying `release.yml` with a throwaway tag before the next real one is now part of the checklist whenever that workflow changes, because four of its actions are unreachable from CI. |
| SB-30 | Database and object store not publicly exposed | 10 | Compose port-exposure test | **TESTED** | `DeploymentTests` reads `compose.yaml`: neither the database nor the object store declares a `ports:` block at all, and every port that *is* published is bound to `127.0.0.1` rather than to every interface the host has. Confirmed against the running stack too — `curl` to 5432 and 9000 on the host is refused while the API answers on 8080. A test rather than a sentence in a document, because a published database port works perfectly until somebody finds it. |
| SB-31 | Non-root containers, no Docker socket | 10 | Container inspection test | **TESTED** | Two checks, in two places. `DeploymentTests` reads the Dockerfiles and fails if any lacks a `USER` line, and fails if any deployment file mentions `docker.sock`. The `supply-chain` workflow then inspects the built images and fails if `Config.User` is empty, `root`, or `0` — confirmed locally: all three report `1654`. The application containers additionally run read-only, with every capability dropped and `no-new-privileges`. |
| SB-32 | Secrets never in images or the repository | 0, 10 | Repository and image secret scan | **TESTED** | Nothing is baked into an image: every secret arrives as an environment variable, and `DeploymentTests.no_deployment_file_carries_a_secret` fails on a secret-shaped name assigned anything other than a variable reference. The real `docker/.env` is refused by `.gitignore`, and a test asserts that it still is — the committed `.env.example` names the variables and holds no values. **The image half is by construction rather than by scan**: no `COPY` brings a secret in and no `ENV` sets one, but no image scanner runs in CI, so a secret introduced some other way would not be caught. |
| SB-33 | Backup and tested restore | 10 | Destroy-and-restore drill | **TESTED** | `RestoreDrillTests.an_installation_survives_losing_its_database_entirely` writes a published record with its approval, an account, an audit entry and an artefact to real PostgreSQL and real MinIO, takes a backup, creates and migrates a **different, empty** database — deleting rows would leave everything a migration created — restores, and checks all of it came back including the evidence bytes read out of the object store. The same drill was then performed by hand against the Compose stack: volume destroyed, migrated, restored, signed in. Restoring over a populated installation and restoring an unknown reference are both refused, and both are tested. |

---

## Phase 11 scenario coverage

The eight scenarios `info.md` requires, and the controls that must be `TESTED` before each is
considered exercised.

| # | Scenario | Controls |
|---|---|---|
| 1 | Cross-user, team, and project access incl. search, caches, attachments, exports | SB-11, SB-12 — **exercised** |
| 2 | Revoked permissions take effect | SB-14, SB-11 — **exercised** |
| 3 | Malicious source and tool content | SB-01 to SB-07 — **exercised**, now including the shipped instruction files |
| 4 | Sensitive-data leakage | SB-17, SB-18, SB-19 and SB-26 — **exercised** |
| 5 | Approval revision mismatch | SB-23, SB-24 — **exercised** |
| 6 | Resource exhaustion | SB-21, SB-22 — **exercised** |
| 7 | Recovery | SB-33 — **exercised** |
| 8 | Retention and deletion across copies | SB-27 (audit events, evidence, backups, exports, deleted projects) — **exercised** |

**All eight scenarios are fully exercised.** Scenarios 1, 2, 3 and 5 run against real PostgreSQL through the real pipeline. Phase 10 added 6 and 7: every limit now exists and is tested, and the destroy-and-restore drill runs in CI as well as having been performed by hand against the Compose stack. Scenario 4 is fully exercised: SB-18 blocks and redacts personal data on the AI channel, honours an owner-approved bounded scope, and still refuses a secret inside one. Scenario 8 is exercised for every copy this build makes a durable one of — audit events, evidence, backups, and exports — plus a project's own immediate deletion; application log retention remains a named residual, a container log-driver setting outside this codebase rather than something untested by omission — see the release-readiness note.

---

## Change log

| Date | Change |
|---|---|
| 2026-09-01 | Created in Phase 0. All 33 rows `NOT IMPLEMENTED`; no scenario exercised. |
| 2026-09-01 | Phase 1. SB-20, SB-23, SB-24, SB-25, SB-26 moved to `IMPLEMENTED`: the domain half of each is tested and passing. **Nothing is `TESTED`.** Scenario 5 (approval revision mismatch) is exercised at the domain level only. |
| 2026-09-01 | Phase 2. SB-07, SB-10, SB-11, SB-17, SB-19 moved to `IMPLEMENTED`: the application-layer half of each is tested against fake ports. **Nothing is `TESTED`.** No control has been exercised against a real database, a real MCP transport, or a real secret scanner. |
| 2026-09-01 | Phase 3. **First two rows reach `TESTED`:** SB-24 and SB-25 pass end to end against real PostgreSQL, including the storage-layer refusal to rewrite a stored revision. SB-05, SB-12 and SB-21 move to `IMPLEMENTED` for the parts persistence covers. Still untested: everything needing authentication, an MCP transport, or a secret scanner. |
| 2026-09-01 | Phase 4. SB-11, SB-12, SB-14 and SB-15 reach `TESTED`; SB-08, SB-09, SB-13 and SB-16 move to `IMPLEMENTED`. **Scenarios 1 and 2 are exercised.** SB-17 is downgraded in substance rather than status: the redactor that ships today redacts nothing, and the note now says so. |
| 2026-09-01 | Phase 5. SB-19, SB-20 and SB-23 reach `TESTED`, taking the total to nine. **Scenario 5 is exercised.** Audit entries now carry structured, capped metadata, so the approver, the exact approved revision, the timestamp, and whether the approver wrote the draft are in the audit history rather than only on the record. |
| 2026-09-01 | Phase 6. Seven rows reach `TESTED` — SB-01, SB-02, SB-03, SB-04, SB-05, SB-06 and SB-17 — taking the total to sixteen, more than half. **Scenario 3 is exercised except for SB-07.** The placeholder redactor is gone: a real scanner and redactor now share one rule set, and the pipeline refuses inbound content carrying a credential rather than storing it. Source synchronisation was not delivered; `sync_sources`, `compare_snapshots` and `analyze_change_impact` throw until an adapter exists. |
| 2026-09-01 | Phase 6 debt closed. SB-22 reaches `TESTED`, taking the total to seventeen. Source synchronisation now works against a mounted working copy, read as files rather than by running git; packed objects are reported as unsupported by name rather than as absent history. |
| 2026-09-02 | Phase 7. Six rows reach `TESTED` — SB-07, SB-08, SB-09, SB-10, SB-13 and SB-16 — taking the total to twenty-three. **Scenario 3 is now fully exercised.** The MCP tool surface exists and is pinned to the allow-list, and a cross-surface test reads one record as an authorised human over HTTP while the same call on the AI channel is refused until the project owner opts in. SB-21 and SB-26 stay `IMPLEMENTED`: rate limiting covers only the credential endpoints, there is no concurrency cap, and no test yet asserts draft invisibility through a specific surface. |
| 2026-09-03 | Phase 8. SB-26 reaches `TESTED`, taking the total to twenty-four; only SB-21 remains `IMPLEMENTED` but unproven. A draft written on top of a published record is proved invisible over HTTP and on the AI channel, and a viewer session is refused approval, correction, publication, archiving, audit reading, membership listing, and every provisioning operation. SB-14 gains a residual risk it did not have before there was a browser: the web client holds a refresh token in `sessionStorage`, because the server issues bearer tokens rather than setting a cookie. |
| 2026-09-03 | Phase 9 walkthrough completed in both hosts. Claude Code and Codex each performed search, analyse, and draft against a local instance over the stdio transport with a machine token; both drafts landed as Drafts awaiting a person, and neither host could reach a human-gated operation because neither was told one exists. |
| 2026-09-03 | Phase 9. No row changes status; four gain evidence. SB-07 is now proved over the real stdio transport rather than only in memory, SB-09 in the process shape a plugin actually runs in, SB-02 in both directions — a shipped instruction file cannot name an operation people alone may perform — and SB-14 covers the machine tokens the plugins present. The hole those tokens closed was real and was ours: the stdio server took its caller identifier from an environment variable, so anybody who could start the process could start it as anybody. Shipping plugin packages documenting that variable would have shipped impersonation as configuration. |
| 2026-09-03 | Phase 10. Six rows reach `TESTED` — SB-21, SB-28, SB-30, SB-31, SB-32 and SB-33 — taking the total to thirty, and scenarios 6 and 7 are now exercised. SB-29 is `IMPLEMENTED`: SBOMs are generated per host but no release has carried one, and nothing is signed. Only SB-18 and SB-27 remain unimplemented, both Phase 11. Standing the stack up found three real defects that no test had: the MCP server's HTTP transport had never once started, because `MapMcp` was called without `WithHttpTransport`; the backup volume arrived owned by root and the first backup failed; and `restore_system` could never have succeeded, because a restore from total loss runs against a database with no memberships to authorise a caller against. |
| 2026-09-03 | Phase 11. SB-18 reaches `TESTED`, taking the total to thirty-one: personal data is blocked and redacted on the AI channel by default, an owner-approved bounded scope lifts that and only that, and a secret is still refused inside one. **Scenario 4 is now fully exercised.** SB-27 moves to `IMPLEMENTED`: retention is enforced for audit events, orphaned evidence, and backups, run from a new operator-scheduled console command rather than an in-process scheduler, closing the specific gap Phase 10 named — backup retention was recorded but not enforced. Not proven and named as residual rather than hidden: exports have no stored artefact yet to purge, application log retention is a container log-driver setting outside this codebase, and a deleted-project sweep has no trigger because no operation deletes a project at all in v1. Zero rows remain `NOT IMPLEMENTED`. |
| 2026-09-10 | Machine tokens bound to a workspace as well as a user. No row changes status; SB-11 and SB-14 gain evidence, and one real gap closes that neither had covered: a token carried only its owner, so one credential in a global plugin configuration reached every workspace that person belonged to, from whichever checkout an assistant was launched in — with the server answering every call correctly, because every membership behind it was real. The credential now carries a workspace, the authorization service refuses a mismatch before anything is read, and tokens issued before this are refused rather than adopted into a workspace nobody granted. `MachineTokenScopeTests` is new, `StdioPluginTests` and `PluginPackageTests` grew, and the Codex package stopped holding literal settings in an account-wide file. |
| 2026-09-03 | Closing the v1 gaps named after Phase 11. SB-27 reaches `TESTED`, taking the total to thirty-two: exports now write an actual copy so the retention sweep has something to purge, and a new `delete_project` operation removes a project's rows and evidence bytes immediately, making the deleted-project sweep unneeded rather than untested. **Scenario 8 is now fully exercised.** None of this widened the AI surface or touched an existing control's status: team administration, a second workspace (sponsored by an existing administrator, not a new installation-wide role), and email delivery of setup and recovery tokens are new human-gated capabilities with their own tests, not security-control changes. Source synchronisation gained an opt-in GitHub API client behind the same `ISourceSystemClient` port Phase 6 already used, exercised without touching SB-01 through SB-07: outbound access still refuses `api.github.com` until an operator adds it to the allow-list, proven by a test asserting the fake handler underneath is never called when it is absent. |
| 2026-09-04 | The first CI run on Linux found a real SB-05 escape that a green Windows suite had hidden: `PathGuard` resolved only the last path component, so a file behind a symlinked parent directory was accepted. Fixed to resolve every component, with a test that names the case and a mutation check. No row changes status — SB-05 was and remains `TESTED` — but it was `TESTED` on the strength of a test that had never executed its own assertion on this platform, which is worth recording. |
| 2026-09-05 | Evidence gained a write side. `download_evidence`, backup and restore, the retention sweep and the isolation suite had all existed since Phase 3 over a store nothing outside a test could ever put a byte into, because no operation, endpoint or screen called `IEvidenceStore.StoreAsync`. `capture_evidence` and `list_evidence` close that, human-only and through the pipeline, so a file carrying a credential is Blocked with nothing written (SB-17). No row changes status, but SB-12's evidence does: the attachment path is now covered for evidence a person actually attached rather than only for evidence a test seeded. |
| 2026-09-06 | First release cut. Tag `v1.0.0` from `6a60a48`, run 34041250645. SB-29 stays `IMPLEMENTED` and the reason is now the last step rather than the whole of it: the images, the per-image SBOMs and the OIDC attestations exist and verify from outside the workflow, against a negative control, but the release is still a **draft**, so no consumer can obtain any of it. An earlier tag was cut and withdrawn rather than quietly replaced, because using the system found three defects reading it had not — a symlink escape past the path guard that only Linux exposed (recorded above), a password beginning with `@` parsed by the console as a response file, which broke the first command any new installation runs, and the missing evidence write side. The destroy-and-restore drill then found a fourth, in the shipped stack rather than in code: `EvidenceStoreOptions.UseServerSideEncryption` defaults to true and the bundled MinIO had no key, so every upload failed with a 500. Fixed by giving MinIO a key rather than by turning encryption off, with a `DeploymentTests` check for it, and the drill re-run to completion from an empty stack. |
| 2026-09-06 | **SB-29 reaches `TESTED`, taking the matrix to 33 of 33.** The draft above was withdrawn rather than published, for a reason that had nothing to do with the binaries: its source archive carried the Compose file from before the MinIO key, so an operator deploying from the tag rather than from `main` would have met the same 500 the drill had just found. Re-cut at `9a8ebf0`, run 34045844221, published, and verified after publication from outside the workflow — three images, the `linux-x64` archive byte for byte against `SHA256SUMS`, a per-image SBOM attested as CycloneDX, and a deliberately wrong `--owner` refused. Smoke tests were re-run for this tag on `win-x64` and `linux-x64`: eighteen AI-exposed operations, exit 0, and neither `capture_evidence` nor `publish_record` on that surface. The rest of the checklist is carried over from `6a60a48`, and is said to be carried over rather than re-run, because the commits between the two changed documentation, a comment, the Compose file and two tests, and no product code. Four platforms remain built-but-never-run and `linux/arm64` images are still not built; the release notes say so verbatim. |
| 2026-09-10 | Phase 12A and 12B. **No row changes status; the matrix stays 33 of 33.** SB-15, SB-27 and SB-29 gain evidence, and in each case what they gain is the closing of something this file had recorded as the operator's rather than as a control. SB-27: the retention sweep is now scheduled by the shipped stack (`retention --every 24h`), so "whether anything runs it" stops being a question, and `RetentionScheduleTests` asserts the scheduled pass and the single pass are the same sweep rather than two paths that agree. SB-15: with no SMTP configured v1 wrote every recovery token into the application log unconditionally, and `Email:AllowTokensInLog` is false now — the risk is closed at the source rather than accepted, and the deliberate consequence is that self-service recovery does not work until SMTP or the opt-in is set. SB-29: `linux/arm64` images are built, published under the same tag, and started, which closes half of what `v1.0.0`'s release notes had to state as unverified. What is left accepted, in writing and dated, is in `docs/security/release-readiness.md`: option 2 for application logs, and four RIDs that keep shipping without ever having been run. Phase 12C is not started and its two ADRs are proposals; no control here anticipates them, and ADR-0012 says explicitly that embedding text is a third egress path needing rows of its own rather than being read as covered by SB-17 and SB-18. |
