// Phase 0 scaffold. Commands — migrate, seed, health, export, backup/restore, sync,
// reindex — are built in Phase 7.

Console.Error.WriteLine(
    "DevBuddy CLI is scaffolding only — no commands are implemented. See docs/plan.md, Phase 7.");
return 1;
