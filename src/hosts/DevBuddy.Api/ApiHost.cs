namespace DevBuddy.Api;

/// <summary>
/// A named handle on this assembly for <c>WebApplicationFactory</c>.
/// <para>
/// The obvious choice is the generated <c>Program</c> class, but every host in this repository has
/// one of those, and a test project that references two hosts cannot then say which it means. A
/// type with a name of its own costs nothing and removes the ambiguity.
/// </para>
/// </summary>
public sealed class ApiHost;
